# Sample environment configuration for the MCP SAP server
TENANT_ID=GUID
AUDIENCE=GUID
# Optional: override default port
PORT=3000
# Number of trusted proxy hops; 1 is correct for Dev Tunnels/nginx/Cloudflare
TRUST_PROXY=1

# Optional: accept additional delegated scopes (space or comma separated)
# ALLOWED_SCOPES=Mcp.Access access_as_mcp
# Optional: accept app roles for client-credential tokens
# ALLOWED_ROLES=McpServer.Invoke,McpServer.Read
# Optional: comma separated list of origins allowed for CORS
# CORS_ORIGIN=http://localhost:3000,https://contoso.com
# Optional: tune rate limiting (defaults shown)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=100
